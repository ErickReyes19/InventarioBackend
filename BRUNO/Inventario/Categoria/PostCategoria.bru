meta {
  name: PostCategoria
  type: http
  seq: 5
}

post {
  url: {{url}}/api/Categoria
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "nombre": "Libro",
    "descripcion":"categoria de libros"
  }
}
